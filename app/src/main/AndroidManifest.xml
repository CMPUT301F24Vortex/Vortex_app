<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vortex_App"
        tools:targetApi="31">

        <!-- Notifications -->
        <activity
            android:name=".view.notification.NotificationsActivity"
            android:exported="false"
            android:launchMode="singleTop"/>
        <activity
            android:name=".view.notification.NotificationDetailActivity"
            android:exported="false"
            android:launchMode="singleTop"/>

        <!-- Events -->
        <activity
            android:name=".view.event.ManageEventsActivity"
            android:exported="false"
            android:launchMode="singleTop"/>
        <activity android:name=".view.event.EventActivity" />
        <activity android:name=".view.event.EventInfoActivity" />

        <!-- Organizer Activities -->
        <activity android:name=".view.organizer.OrganizerInfo"/>
        <activity android:name=".view.organizer.OrganizerMenu"/>
        <activity android:name=".view.event.AddEvent"/>
        <activity android:name=".view.organizer.OrganizerActivity"
            android:exported="false"/>
        <activity android:name=".view.organizer.OrgNotificationsActivity" />
        <activity android:name=".view.organizer.OrgNotificationDetailsActivity" />
        <activity android:name=".view.organizer.OrgCreateNotificationActivity" />

        <activity android:name=".view.qrcode.OrgQRCodeActivity"
            android:exported="false"/>
        <activity android:name=".view.entrant.CancelledEntrantsActivity" />
        <activity android:name=".view.entrant.SelectedEntrantsActivity" />
        <activity android:name=".view.entrant.FinalEntrantsActivity" />
        <activity android:name=".view.waitinglist.OrgWaitingListActivity" />
        <activity android:name=".view.location.LocationActivity" />

        <!-- Waiting List -->
        <activity android:name=".WaitingListActivity" />

        <!-- Profile -->
        <activity
            android:name=".view.profile.ProfileActivity"
            android:exported="false"
            android:launchMode="singleTop"/>
        <activity
            android:name=".view.profile.EditProfileActivity"
            android:exported="false" />

        <!-- Entrant -->
        <activity
            android:name=".view.entrant.EntrantActivity"
            android:exported="false" />

        <!-- Signup -->
        <activity android:name=".view.activity.SignupActivity" />

        <!-- QR Scanner -->
        <activity
            android:name=".view.qrcode.QrScannerActivity"
            android:exported="false" />
        <!-- If using CustomScannerActivity -->
        <activity
            android:name=".view.qrcode.CustomScannerActivity"
            android:exported="false" />

        <!-- Firebase Messaging Service -->
        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>


        <!-- Admin activities -->
        <activity android:name=".view.admin.AdminMainscreen" android:exported="false"/>
        <activity android:name=".view.admin.AdminFacilityScreen" android:exported="false"/>
        <activity android:name=".view.admin.AdminEventScreen" android:exported="false"/>
        <activity android:name=".view.admin.AdminProfileScreen" android:exported="false"/>
        <activity android:name=".view.admin.AdminProfileViewer" android:exported="false"/>
        <activity android:name=".view.admin.AdminEventInfoScreen" android:exported="false"/>


        <!-- Main Activity -->
        <activity
            android:name=".view.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
